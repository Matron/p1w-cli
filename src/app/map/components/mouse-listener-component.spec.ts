import { MouseListenerComponent } from './mouse-listener-component';

describe('MouseListenerComponent', () => {
  it('should create an instance', () => {
    expect(new MouseListenerComponent()).toBeTruthy();
  });
});
