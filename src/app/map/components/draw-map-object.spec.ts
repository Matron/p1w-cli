import { DrawMapObjectComponent } from './draw-map-object';

describe('DrawMapObject', () => {
  it('should create an instance', () => {
    expect(new DrawMapObjectComponent()).toBeTruthy();
  });
});
